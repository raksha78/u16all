language: none
services:
 - postgres
build:
 pre_ci_boot:
  image_name: drydock/u16all
  image_tag: master
  pull: false
 ci:
  - sudo su postgres -c "psql -c '\l'"
  - shippable_service postgres stop
