language: none
services:
 - selenium
build:
 pre_ci_boot:
  image_name: raksh78/latest16
  image_tag: latest
  pull: false
 ci:
  - curl --head http://localhost:4444
  - shippable_service selenium stop
